/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ["img.freepik.com","i.ibb.co.com"],
  },
  reactCompiler: true,
};

export default nextConfig;
