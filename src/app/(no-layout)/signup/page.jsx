import RegisterPage from '@/app/components/AuthComponent/Register';
import React from 'react';

const page = () => {
    return (
        <div>
            <RegisterPage/>
        </div>
    );
};

export default page;